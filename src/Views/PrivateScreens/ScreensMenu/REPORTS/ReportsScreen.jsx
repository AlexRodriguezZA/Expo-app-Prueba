import { Text, Surface } from "react-native-paper";

const ReportsScreen = () => {
  return (
    <Surface style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#ffffff" }}>
      <Text style={{ fontSize: 24, fontWeight: "bold" }}>Reports Screen</Text>
    </Surface>
  );
};

export default ReportsScreen;
